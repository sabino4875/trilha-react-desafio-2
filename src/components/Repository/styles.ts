import { styled } from "styled-components";

export const Repositories = styled.div<object>`
    display: flex;
    flex-direction: column;
    align-items: center;
`

export const RepositoryTitle = styled.h2`
    font-size: 1.5rem;
    color: #FFFFFF;
    font-weight: 900;
    text-align: center;
`

export const RepositoriesItemsContainer = styled.div<object>`
    display: flex;
    flex-direction: column;
    width: 100%;
`

export const RepositoriesInputContainer = styled.div<object>`
    width: 100%;
    display: flex;
    flex-direction: column;
`

